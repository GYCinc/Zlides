{
  "id": "LAC",
  "name": "LAC",
  "description": "gitEnglish-branded Lesson Asset Catalog: linguistic assets and phenomena from a session",
  "prompt": "DOCUMENT & PRINT LAYOUT SPECIFICATIONS:\n1. PAGE DIMENSIONS: Single continuous A4 document layout. Document `.container` MUST have `max-width: 800px; width: 100%; margin: 0 auto; padding: 24px;`.\n2. PRINT CSS: Include `@media print { @page { size: A4 portrait; margin: 12mm; } body { background: #fff !important; color: #111 !important; } .container { max-width: 100% !important; padding: 0 !important; } .card, .summary-box, .section-break { break-inside: avoid; page-break-inside: avoid; } }`.\n3. RESPONSIVE GRIDS: `.grid-3` MUST use `display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px;`. Cards wrap cleanly without horizontal clipping.\n\nDOCUMENT STRUCTURE & EXACT DATA MAPPING:\nCreate a Lesson Asset Catalog (LAC) for the provided session. Top `<header>` contains lesson title and date. Use `<section>` blocks for content. Integrate `<div class=\"summary-box\">` for summary with an absolutely positioned `.summary-label` reading Summary_Log. TYPOGRAPHY FLOOR: no font anywhere smaller than 15px. When listing items, use `<section class=\"section-break\">` and a `<div class=\"section-header\">` with `<span class=\"count-label\">` reflecting item count.\n\nCARD INTERNAL STRUCTURE (every card, no exceptions):\n(1) Item term alone in `<h3 class=\"sm\">`,\n(2) `<div class=\"card-meta\">` of metadata chips,\n(3) `<p class=\"def\">` for definition/main text,\n(4) `<p class=\"ex\">` for learner quote, impact, or example (italic, with top divider line).\n\nFIELD-BY-FIELD SECTION SPECIFICATIONS:\n- Vocabulary: heading = word/item; card-meta contains `<span class=\"pos-tag\">pos</span>` (part of speech ALWAYS abbreviated n/v/adj/adv) and `<span class=\"rank-badge\">exact_coca_rank</span>`; def = concise definition (from def or details); ex = learner note/quote. Wrap in `<div class=\"grid-3\">` (max 3 columns).\n- Phrasal Verbs: heading = item; card-meta contains Latinate synonym chip (`latinateSynonym`) and usage split chip (`usagePercentVsLatinate`); def = main explanation/meaning (from details or impact); ex = impact of phrasal verb vs single-word alternative.\n- Collocations: heading = item; def = details; ex = learner quote or note if available.\n- Idioms & Phrases: heading = item; card-meta contains `<span class=\"chip\">register</span>`; def = details; ex = implicitMeaning.\n- Grammar: FULL-WIDTH horizontal cards, one per row (never inside row-2/multi-column). After def, render labeled rows using `<div class=\"kv\"><span class=\"kv-tag\">ALT</span><span>second_alternative</span></div>`, EX rows for logical_example_1 and logical_example_2, and `<div class=\"kv trap\"><span class=\"kv-tag\">TRAP</span><span>avoid_this_trap</span></div>`.\n- Focus Points: FULL-WIDTH single-column horizontal rows; heading = item; def = details.\n- Practice & Self-Study: Pair In-Class Practice + Self-Study inside `<div class=\"row-2\">` side-by-side.\n- Action Items: Pair Student Action Items + Teacher Action Items inside `<div class=\"row-2\">` side-by-side.\n\nICONS: ONLY use premium, modern inline SVGs (Lucide style). NEVER use basic emojis. Professional document layout suitable for printing."
}
