{
  "id": "LAC",
  "name": "LAC",
  "description": "gitEnglish-branded Lesson Asset Catalog: linguistic assets and phenomena from a session",
  "prompt": "Create a Lesson Asset Catalog (LAC) for the provided session. Structure with a top `<header>` (containing the lesson title and date). Use `<section>` blocks for content. Integrate `<div class=\"summary-box\">` for the session summary with an absolutely positioned `.summary-label` reading Summary_Log. TYPOGRAPHY FLOOR: no font anywhere may be smaller than 13px. VOCABULARY/PHRASAL VERBS/COLLOCATIONS/GRAMMAR/IDIOMS/FOCUS POINTS: when listing items, use `<section class=\"section-break\">` and a `<div class=\"section-header\">` with a `<span class=\"count-label\">` reflecting the actual item count. CARD INTERNAL STRUCTURE (every card, no exceptions — never a flat blob of text): (1) the item term alone in `<h3 class=\"sm\">`, (2) a `<div class=\"card-meta\">` of chips, (3) `<p class=\"def\">` for the core definition, (4) `<p class=\"ex\">` for the learner quote/example (rendered smaller, italic, with a top divider line). For Vocabulary specifically: heading is the WORD ONLY; the card-meta row contains `<span class=\"pos-tag\">n</span>` (part of speech ALWAYS abbreviated as n / v / adj / adv — NEVER spelled out) and `<span class=\"rank-badge\">14602</span>` (the exact_coca_rank); def = concise 4-5 word definition; ex = the learner-specific note or quote. Wrap vocabulary in `<div class=\"grid-3\">` so it spans a MAXIMUM of 3 columns — never 4. For Phrasal Verbs: card-meta contains the Latinate synonym chip and the usage split chip (e.g. `evade` + `95 / 5`); def = meaning; ex = the impact of the phrasal verb vs the single-word alternative. For `idiomsAndPhrases`: MUST include all four fields — `item`, `details`, `register`, `implicitMeaning`. card-meta contains the `register` chip (e.g. `informal`); def = `details`; ex = `implicitMeaning`. For Grammar: FULL-WIDTH horizontal cards, one per row — NEVER inside row-2 or any multi-column grid. After the def, render labeled rows using `<div class=\"kv\"><span class=\"kv-tag\">ALT</span><span>…</span></div>` for second_alternative, one `<div class=\"kv\">` per example tagged EX, and `<div class=\"kv trap\"><span class=\"kv-tag\">TRAP</span><span>…</span></div>` for avoid_this_trap. FOCUS POINTS also MUST be full-width single-column horizontal rows — never multi-column. LAYOUT: pair only the short sections (Phrasal Verbs + Collocations, In-Class Practice + Self-Study) inside `<div class=\"row-2\">`. ICONS: ONLY use premium, modern inline SVGs (like Lucide outline icons). NEVER use emojis. Professional document layout suitable for printing."
}
